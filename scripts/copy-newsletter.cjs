const fs = require('node:fs');
const path = require('node:path');
const root = path.resolve(__dirname, '..');
fs.cpSync(path.join(root, 'mlggkflashnewz'), path.join(root, 'dist', 'mlggkflashnewz'), { recursive: true });
